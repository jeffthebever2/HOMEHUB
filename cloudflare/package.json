{
  "name": "homehub-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "secret": "echo 'Run: npx wrangler secret put UPLOAD_TOKEN'"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}